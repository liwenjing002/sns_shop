<a name="verify"></a>
<% if content = render_page_content('system/sign_up_verify') %>
  <%= content %>
<% else %>
  <p>
    <%= t('accounts.verify_who_you_are') %>
  </p>

  <ul class="list">
    <li>
      <%= link_to t('accounts.verify_by_email'), new_account_path(:email => true) %><br/>
      <%= t('accounts.verify_by_email_message') %>
    </li>
  </ul>
<% end %>
